# ─── Active Directory ────────────────────────────────────────────────────────
# FQDN of the target AD domain (e.g. corp.example.com)
AD_DOMAIN=corp.example.com

# AD domain controller hostname or IP used by PowerShell remoting (optional;
# if omitted, PowerShell will use the default DC discovery)
AD_SERVER=dc01.corp.example.com

# Distinguished Name of the OU where new users will be created
AD_BASE_OU=OU=ScimUsers,DC=corp,DC=example,DC=com

# Temporary initial password assigned to new AD accounts.
# Must satisfy your domain's password complexity policy.
# The account is created with ChangePasswordAtLogon=$false because
# authentication is federated via the identity provider.
AD_DEFAULT_PASSWORD=Change@Me123!

# ─── Authentication ───────────────────────────────────────────────────────────
# Password for HTTP Basic authentication (Authorization: Basic base64(user:API_KEY)).
# The username field is ignored — set any value in the Okta SCIM app.
# Use a long random string in production (e.g. openssl rand -hex 32).
API_KEY=replace-with-a-strong-secret

# ─── TLS / SSL ───────────────────────────────────────────────────────────────
# Absolute paths to PEM-encoded certificate and private key files.
# To generate a self-signed cert for testing:
#   openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes
SSL_CERT_PATH=/etc/scim-ad-bridge/cert.pem
SSL_KEY_PATH=/etc/scim-ad-bridge/key.pem

# ─── Server ──────────────────────────────────────────────────────────────────
PORT=3000
